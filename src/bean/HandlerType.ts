enum HandlerType {
  none = 'none',
  mysql = 'mysql',
  postgresql = 'postgresql',
  mssql = 'mssql',
  oracle = 'oracle',
  sqlite = 'sqlite',
  cassandra = 'cassandra',
  clickhouse = 'clickhouse'
}

export default HandlerType;
